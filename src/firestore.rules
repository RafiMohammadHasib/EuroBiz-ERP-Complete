
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // ---------------------------
    // --- Helper Functions ---
    // ---------------------------
    function isSignedIn() {
      return request.auth != null;
    }

    function isAdmin() {
      return isSignedIn() && get(/databases/$(database)/documents/user_roles/$(request.auth.uid)).data.role == 'admin';
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    // --- Role Management ---
    match /user_roles/{uid} {
      allow read, write: if isAdmin();
    }

    // --- Business-Wide Data ---
    // Admins have full access. All other authenticated users have read-only access.
    match /products/{productId} { allow read, write: if isSignedIn(); }
    match /settings/{settingId} { allow read, write: if isSignedIn(); }
    match /distribution_channels/{dcId} { allow read, write: if isSignedIn(); }
    match /distributors/{distributorId} { allow read, write: if isSignedIn(); }
    match /suppliers/{supplierId} { allow read, write: if isSignedIn(); }
    match /purchaseOrders/{poId} { allow read, write: if isSignedIn(); }
    match /sales_commissions/{commissionId} { allow read, write: if isSignedIn(); }
    match /invoices/{invoiceId} { allow read, write: if isSignedIn(); }
    match /rawMaterials/{materialId} { allow read, write: if isSignedIn(); }
    match /finishedGoods/{goodId} { allow read, write: if isSignedIn(); }
    match /productionOrders/{prodId} { allow read, write: if isSignedIn(); }
    match /commissions/{commissionId} { allow read, write: if isSignedIn(); }
    match /salary_payments/{paymentId} { allow read, write: if isSignedIn(); }


    // --- User Profiles & Nested Data ---
    match /salespeople/{salespersonId} {
      allow get, update, delete: if isOwner(salespersonId);
      allow create: if isOwner(salespersonId) && request.resource.data.id == salespersonId;

       match /commissions/{commissionId} {
        allow read, write: if isOwner(salespersonId);
      }
    }

    match /customers/{customerId} {
      allow get, update, delete: if isOwner(customerId);
      allow create: if isOwner(customerId) && request.resource.data.id == customerId;
      allow list: if isSignedIn();

       match /invoices/{invoiceId} {
        allow read, write: if isOwner(customerId);

        match /outstanding_dues/{dueId} {
          allow read, write: if isOwner(customerId);
        }
      }
    }
  }
}
